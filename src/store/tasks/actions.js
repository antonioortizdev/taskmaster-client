import { CREATE_TASK, FIND_TASKS } from "../../constants";

export const actions = {
  [FIND_TASKS]: ({ commit }, filters) => {
    // action implementation
  },
  [CREATE_TASK]: ({ commit }, taskData) => {
    // action implementation
  }
}